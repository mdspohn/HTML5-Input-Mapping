class Gamepad extends Device {
    constructor() {
        super();
    }

    use(config) {
        this.index = config.index;
    }

    enable() {
        super();
    }

    disable() {
        super();
    }
}